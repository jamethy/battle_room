
#ifndef BATTLE_ROOM_COMMAND_TYPE_H
#define BATTLE_ROOM_COMMAND_TYPE_H

namespace BattleRoom {
    enum CommandType {

        Aim,
        ShootCharge,
        ShootRelease,
        JumpCharge,
        JumpRelease,
        Freeze,
        Unfreeze,
        Invalid
    }; // CommandType enum
} // BattleRoom namespace
#endif // BATTLE_ROOM_COMMAND_TYPE_H
