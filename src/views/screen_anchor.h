#ifndef BATTLE_ROOM_SCREEN_ANCHOR_H
#define BATTLE_ROOM_SCREEN_ANCHOR_H

#include <string>

namespace BattleRoom {

    enum ScreenAnchor {
        TopLeft, TopRight, BottomLeft, BottomRight, Center
    }; // ScreenAnchor enum

} // BattleRoom namespace
#endif // BATTLE_ROOM_SCREEN_ANCHOR_H
