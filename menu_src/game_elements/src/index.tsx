import * as React from "react";
import * as ReactDOM from "react-dom";
import {GameElementsContainer} from "./components/GameElementsContainer";

ReactDOM.render(
    <GameElementsContainer/>,
    document.getElementById("root")
);
